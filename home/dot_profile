# Login shells. bash loads the environment from .bashrc; any other sh (e.g. the
# one a display manager runs) only needs the environment.
if [ -n "${BASH_VERSION:-}" ]; then
    [ -f "${HOME}/.bashrc" ] && . "${HOME}/.bashrc"
else
    . "${HOME}/.local/share/shell/variables.sh"
fi
